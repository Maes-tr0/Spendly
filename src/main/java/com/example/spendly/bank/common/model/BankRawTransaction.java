package com.example.spendly.bank.common.model;

/**
 * Marker interface for bank-specific raw transaction models.
 * <p>
 * Raw transactions keep the original structure of a specific bank
 * before mapping to the common ParsedTransaction model.
 */
public interface BankRawTransaction {
}